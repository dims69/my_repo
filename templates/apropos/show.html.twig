{% extends 'base.html.twig' %}

{% block title %}Apropos{% endblock %}

{% block body %}
    <h1>Apropos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ apropo.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ apropo.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('apropos_index') }}">back to list</a>

    <a href="{{ path('apropos_edit', {'id': apropo.id}) }}">edit</a>

    {{ include('apropos/_delete_form.html.twig') }}
{% endblock %}
