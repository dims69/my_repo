<!DOCTYPE html>
<html>
	{{ form_start(form, {'action': path('valid_competence'),'attr': {'novalidate': 'novalidate' } }) }} 
	{{ form(form) }}
	{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
	{{ form_widget(form) }}
	{{ form_end(form) }}
</html>