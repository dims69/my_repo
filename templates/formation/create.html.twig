<!DOCTYPE html>
<html>
	<h1>Création d'une nouvelle formation</h1>
	{{ form_start(form, {'action': path('valid_formation'),'attr': {'novalidate': 'novalidate' } }) }} 
	{{ form(form) }}
	{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
	{{ form_widget(form) }}
	{{ form_end(form) }}
</html>